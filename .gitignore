# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependency directories
node_modules/

# Rollup cache and build outputs
.rollup.cache
.cache/
dist/
build/
promptmate-dist/

# Environment variables
.env
.env.local
.env.*.local

# Chrome extension packaging
*.crx
*.zip

# Miscellaneous
.DS_Store
Thumbs.db
__MACOSX/

# IDE/editor files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln

# Private planning docs (kept local, not published)
tasks/
notes/

# Claude Code — contributor-local settings and credentials
.claude/settings.local.json
.claude/.credentials.json

# OAuth signing key for stable dev extension ID (never commit)
promptmate-key.pem

# Google OAuth client credentials download (not a real secret for Chrome extensions,
# but Google's default filename pattern is alarming — keep out of the repo regardless)
client_secret_*.json
